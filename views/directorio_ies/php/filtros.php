<?php 
                /*--------------------------------------------------------------------------------------------------------*/
                /*-----------------------------------------VARIABLES DE BÚSQUEDA------------------------------------------*/
                /*--------------------------------------------------------------------------------------------------------*/

                    $where= "";
                    $estado = $_POST["estado"];
                    $alfabetico = $_POST["alfabetico"];
                    $buscar_texto = $_POST["buscar_texto"];
                    $message = "<h5 class = 'bold'>No se encontraron resultados de su búsqueda</h5>";

                /*---------------------------------------------------------------------------------------------------------------------*/
                /*-----------------------------------------CONDICIONES-----------------------------------------------------------------*/
                /*---------------------------------------------------------------------------------------------------------------------*/

                    if ($estado =="" && $alfabetico =="" && $buscar_texto =="") {
                        $where = "";

                    }else{
                /*---------------------------------------------------------------------------------------------------------------------*/
                /*-----------------------------------------CONDICIONES FILTROS SOLOS---------------------------------------------------*/
                /*---------------------------------------------------------------------------------------------------------------------*/

                        if ($estado !="" && $alfabetico =="" && $buscar_texto =="") {
                            $where = "WHERE estado = '$estado'";
                        }

                        if ($estado =="" && $alfabetico !="" && $buscar_texto =="") {
                            $where = "ORDER BY nombre_institucion = '$alfabetico'";
                        }

                        if ($estado =="" && $alfabetico =="" && $buscar_texto !="") {
                            $where = "WHERE (nombre_institucion LIKE '%".$buscar_texto."%') OR (estado LIKE '%".$buscar_texto."%')" ;
                        }
                    }

                ?>